import { array } from 'property-praxis/helpers/array';
import { module, test } from 'qunit';

module('Unit | Helper | array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = array([42]);
  assert.ok(result);
});
