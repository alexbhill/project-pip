import { isZip } from 'property-praxis/helpers/is-zip';
import { module, test } from 'qunit';

module('Unit | Helper | is zip');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isZip([42]);
  assert.ok(result);
});
