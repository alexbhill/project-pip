import { formatDetails } from 'property-praxis/helpers/format-details';
import { module, test } from 'qunit';

module('Unit | Helper | format details');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDetails([42]);
  assert.ok(result);
});
