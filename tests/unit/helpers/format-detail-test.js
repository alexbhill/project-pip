import { formatDetail } from 'property-praxis/helpers/format-detail';
import { module, test } from 'qunit';

module('Unit | Helper | format detail');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDetail([42]);
  assert.ok(result);
});
