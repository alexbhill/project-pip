<p class="property-click" {{action "setActiveOwner" activeProperty}}>{{activeProperty.owner}} <svg><use xlink:href="#icon-info"></use></svg></p>
<p>{{activeProperty.address}}</p>
<p class="property-click" {{action "setActiveZip" activeProperty}}>Detroit, MI {{activeProperty.zip}}<svg><use xlink:href="#icon-info"></use></svg></p>
