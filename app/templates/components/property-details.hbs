<p class="property-click" {{action "setActiveOwner" activeProperty}}>{{format-details activeProperty.owner}} <svg><use xlink:href="#icon-info"></use></svg></p>
<p>{{format-details activeProperty.address}}</p>
<p class="property-click" {{action "setActiveZip" activeProperty}}>Detroit, MI {{format-details activeProperty.zip}}<svg><use xlink:href="#icon-info"></use></svg></p>
